void setup() { 
  size(500, 500);
  
  line(250, 250, 0, 500);
  
  circle(250, 250, 500);
  
  rect(250, 250, 100, 80);
 
  /*
  ellipse(x, y, w, h);
  triangle(x1, y1, x2, y2, x3, y3);
  quad(x1, y1, x2, y2, x3, y3, x4, y4);
  rect(x, y, w, h, r1, r2, r3, r4);
  square(x, y, w);
  point(x, y);
    *arc
    *curve
  circle
  line
  */
  

  
  
}
